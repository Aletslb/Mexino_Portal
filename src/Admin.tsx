import {useEffect,useState,lazy,Suspense} from 'react';
import type {FormEvent,ReactNode} from 'react';
import {Brand} from './PublicPortal';
import {api,saveRecord,uploadImage} from './api';
import {blankProperty,blankDevelopment,blankLot,emptyCatalog,money} from './model';
import type {Property,Development,Lot,Catalog} from './model';
import PlanView from './PlanView';
const MapView=lazy(()=>import('./MapView'));
type User={email:string;role:string};
type Audit={id:string;record_id:string;actor:string;action:string;created_at:string};
function Field({label,children}:{label:string;children:ReactNode}){return <label className="field"><span>{label}</span>{children}</label>;}
function Select({label,value,options,onChange}:{label:string;value:string;options:string[];onChange:(v:string)=>void}){return <Field label={label}><select value={value} onChange={e=>onChange(e.target.value)}>{options.map(o=><option key={o}>{o}</option>)}</select></Field>;}
function NumberField({label,value,onChange}:{label:string;value:number;onChange:(v:number)=>void}){return <Field label={label}><input type="number" min="0" step="any" required value={value} onChange={e=>onChange(Number(e.target.value))}/></Field>;}
function useDirty(dirty:boolean){useEffect(()=>{const fn=(e:BeforeUnloadEvent)=>{if(dirty){e.preventDefault();e.returnValue='';}};window.addEventListener('beforeunload',fn);return()=>window.removeEventListener('beforeunload',fn);},[dirty]);}
function Commission({value,change}:{value:{commissionType:'Porcentaje'|'Monto';commissionValue:number};change:(v:Partial<Property>)=>void}){return <fieldset><legend>Comisión estimada · uso interno</legend><div className="form-grid"><Select label="Tipo" value={value.commissionType} options={['Porcentaje','Monto']} onChange={v=>change({commissionType:v as Property['commissionType']})}/><NumberField label={value.commissionType==='Porcentaje'?'Porcentaje (%)':'Monto (MXN)'} value={value.commissionValue} onChange={v=>change({commissionValue:v})}/></div></fieldset>;}
function Images({images,onChange,setBusy}:{images:string[];onChange:(v:string[])=>void;setBusy:(v:boolean)=>void}){
  const [error,setError]=useState(''),[uploading,setUploading]=useState(false);
  async function add(files:FileList|null){if(!files)return;setError('');if(images.length+files.length>20){setError('Máximo 20 imágenes.');return;}setBusy(true);setUploading(true);const next=[...images];try{for(const f of Array.from(files)){if(f.size>8*1024*1024)throw new Error('Máximo 8 MB por imagen.');next.push(await uploadImage(f));onChange([...next]);}}catch(e){setError((e as Error).message);}finally{setBusy(false);setUploading(false);}}
  return <fieldset><legend>Fotografías</legend><p className="muted">La primera fotografía será la portada. JPG, PNG o WebP, hasta 8 MB.</p><div className="image-editor">{images.map((src,i)=><div key={src+i}><img src={src} alt={`Fotografía ${i+1}`}/><div><button type="button" disabled={uploading||i===0} onClick={()=>{const a=[...images];[a[i-1],a[i]]=[a[i],a[i-1]];onChange(a);}} aria-label={`Mover fotografía ${i+1} antes`}>←</button><button type="button" disabled={uploading||i===images.length-1} onClick={()=>{const a=[...images];[a[i+1],a[i]]=[a[i],a[i+1]];onChange(a);}} aria-label={`Mover fotografía ${i+1} después`}>→</button><button type="button" disabled={uploading} onClick={()=>onChange(images.filter((_,n)=>n!==i))}>Quitar</button></div></div>)}</div><Field label={uploading?'Subiendo…':'Agregar fotografías'}><input type="file" accept="image/jpeg,image/png,image/webp" multiple disabled={uploading} onChange={e=>{void add(e.target.files);e.target.value='';}}/></Field>{error&&<p className="error" role="alert">{error}</p>}</fieldset>;
}
function PropertyEditor({initial,onDone}:{initial:Property;onDone:()=>void}){
  const [value,setValue]=useState(initial),[dirty,setDirty]=useState(false),[busy,setBusy]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState('');useDirty(dirty);
  const change=(v:Partial<Property>)=>{setValue(x=>({...x,...v}));setDirty(true);};
  const close=()=>{if(!dirty||confirm('¿Salir sin guardar los cambios?'))onDone();};
  async function submit(e:FormEvent){e.preventDefault();setSaving(true);setError('');try{await saveRecord('properties',value);setDirty(false);onDone();}catch(e){setError((e as Error).message);}finally{setSaving(false);}}
  return <form onSubmit={submit} className="editor"><div className="editor-top"><div><p className="eyebrow">PROPIEDADES</p><h1>{initial.revision?'Editar propiedad':'Nueva propiedad'}</h1></div><button type="button" onClick={close} className="secondary">Volver</button></div><fieldset disabled={saving}><legend>Información comercial</legend><Field label="Título"><input value={value.title} maxLength={160} required onChange={e=>change({title:e.target.value})}/></Field><div className="form-grid"><Select label="Tipo" value={value.type} options={['Casa','Local','Terreno']} onChange={v=>change({type:v as Property['type']})}/><Select label="Operación" value={value.operation} options={['Venta','Renta']} onChange={v=>change({operation:v as Property['operation']})}/><NumberField label="Precio (MXN)" value={value.price} onChange={v=>change({price:v})}/><NumberField label="Superficie (m²)" value={value.area} onChange={v=>change({area:v})}/><NumberField label="Recámaras" value={value.bedrooms} onChange={v=>change({bedrooms:v})}/><NumberField label="Baños" value={value.bathrooms} onChange={v=>change({bathrooms:v})}/><Select label="Disponibilidad" value={value.status} options={['Disponible','Vendido','Rentado']} onChange={v=>change({status:v as Property['status']})}/><Select label="Publicación" value={value.publication} options={['Borrador','Publicado','Oculto']} onChange={v=>change({publication:v as Property['publication']})}/></div><Field label="Descripción"><textarea rows={5} maxLength={10000} value={value.description} onChange={e=>change({description:e.target.value})}/></Field></fieldset><Images images={value.images} onChange={v=>change({images:v})} setBusy={setBusy}/><fieldset><legend>Ubicación pública</legend><Field label="Dirección o zona"><input maxLength={500} value={value.address} onChange={e=>change({address:e.target.value})}/></Field><p className="muted">Selecciona un punto en el mapa o captura ambas coordenadas. La ubicación se mostrará al público.</p><div className="form-grid"><Field label="Latitud"><input type="number" min="-90" max="90" step="any" value={value.lat??''} onChange={e=>change({lat:e.target.value===''?null:Number(e.target.value)})}/></Field><Field label="Longitud"><input type="number" min="-180" max="180" step="any" value={value.lng??''} onChange={e=>change({lng:e.target.value===''?null:Number(e.target.value)})}/></Field></div><Suspense fallback={<p>Cargando mapa…</p>}><MapView properties={[]} location={value.lat!==null&&value.lng!==null?[value.lat,value.lng]:null} onPick={(lat,lng)=>change({lat,lng})}/></Suspense><button type="button" className="text-button" onClick={()=>change({lat:null,lng:null})}>Quitar ubicación del mapa</button></fieldset><Commission value={value} change={change}/>{error&&<p className="error" role="alert">{error}</p>}<div className="save-bar"><span>{dirty?'Cambios sin guardar':'Sin cambios pendientes'}</span><button className="button" disabled={busy||saving}>{saving?'Guardando…':'Guardar propiedad'}</button></div></form>;
}
function DevelopmentEditor({initial,onDone}:{initial:Development;onDone:()=>void}){
  const [value,setValue]=useState(initial),[dirty,setDirty]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState(''),[pdf,setPdf]=useState<File|null>(null),[page,setPage]=useState(1);useDirty(dirty);
  const change=(v:Partial<Development>)=>{setValue(x=>({...x,...v}));setDirty(true);};
  async function plan(file:File){setBusy(true);setError('');try{let image=file;if(file.type==='application/pdf'){
    if(file.size>20*1024*1024)throw new Error('El PDF supera 20 MB. Exporta la página como imagen.');
    const pdfjs=await import('pdfjs-dist');const worker=await import('pdfjs-dist/build/pdf.worker.min.mjs?url');pdfjs.GlobalWorkerOptions.workerSrc=worker.default;
    const document=await pdfjs.getDocument({data:await file.arrayBuffer()}).promise;
    try {if(page<1||page>document.numPages)throw new Error(`El PDF tiene ${document.numPages} páginas.`);const p=await document.getPage(page),base=p.getViewport({scale:1}),viewport=p.getViewport({scale:Math.min(2,2400/Math.max(base.width,base.height))});const canvas=window.document.createElement('canvas');canvas.width=viewport.width;canvas.height=viewport.height;await p.render({canvas,canvasContext:canvas.getContext('2d')!,viewport}).promise;const blob=await new Promise<Blob>((resolve,reject)=>canvas.toBlob(b=>b?resolve(b):reject(new Error('No se pudo convertir el plano.')),'image/webp',.9));image=new File([blob],'plano.webp',{type:'image/webp'});}finally{await document.loadingTask.destroy();}
  }change({plan:await uploadImage(image)});}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
  async function submit(e:FormEvent){e.preventDefault();setBusy(true);setError('');try{await saveRecord('developments',value);setDirty(false);onDone();}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
  return <form className="editor" onSubmit={submit}><div className="editor-top"><h1>{initial.revision?'Editar fraccionamiento':'Nuevo fraccionamiento'}</h1><button type="button" className="secondary" onClick={()=>{if(!dirty||confirm('¿Salir sin guardar?'))onDone();}}>Volver</button></div><fieldset disabled={busy}><legend>Desarrollo</legend><Field label="Nombre"><input required maxLength={160} value={value.title} onChange={e=>change({title:e.target.value})}/></Field><Field label="Ubicación"><input value={value.address} maxLength={500} onChange={e=>change({address:e.target.value})}/></Field><Field label="Descripción pública"><textarea rows={4} maxLength={10000} value={value.description} onChange={e=>change({description:e.target.value})}/></Field><Select label="Publicación" value={value.publication} options={['Borrador','Publicado','Oculto']} onChange={v=>change({publication:v as Development['publication']})}/><label className="check"><input type="checkbox" checked={value.collection} onChange={e=>change({collection:e.target.checked})}/> Mexino administrará las mensualidades de este desarrollo</label><p className="muted">Esta configuración se utilizará en la segunda etapa, al registrar ventas y pagos.</p></fieldset><Commission value={value} change={v=>change(v as Partial<Development>)}/><fieldset disabled={busy}><legend>Plano de lotificación</legend><p className="muted">Sube una imagen o selecciona una página de un PDF. Si reemplazas el plano, revisa la posición de todos los contornos.</p><Field label="Seleccionar plano"><input type="file" accept="image/jpeg,image/png,image/webp,application/pdf" onChange={e=>{const f=e.target.files?.[0];if(f){if(f.type==='application/pdf')setPdf(f);else{setPdf(null);void plan(f);}}e.target.value='';}}/></Field>{pdf&&<div className="inline"><NumberField label="Página del PDF" value={page} onChange={setPage}/><button type="button" className="secondary" onClick={()=>void plan(pdf)}>Convertir y subir página</button></div>}{value.plan&&<img className="plan-preview" src={value.plan} alt="Vista previa del plano"/>}</fieldset>{error&&<p className="error" role="alert">{error}</p>}<div className="save-bar"><span>{busy?'Procesando…':dirty?'Cambios sin guardar':''}</span><button className="button" disabled={busy}>Guardar fraccionamiento</button></div></form>;
}
function LotsEditor({dev,lots,onDone,onReload}:{dev:Development;lots:Lot[];onDone:()=>void;onReload:()=>Promise<void>}){
  const [value,setValue]=useState<Lot|null>(null),[drawing,setDrawing]=useState(false),[dirty,setDirty]=useState(false),[busy,setBusy]=useState(false),[error,setError]=useState('');useDirty(dirty);
  const change=(v:Partial<Lot>)=>{setValue(x=>x?{...x,...v}:x);setDirty(true);};
  const choose=(l:Lot)=>{if(dirty&&!confirm('¿Descartar cambios del lote actual?'))return;setValue(l);setDirty(false);setDrawing(false);setError('');};
  async function submit(e:FormEvent){e.preventDefault();if(!value)return;setBusy(true);setError('');try{const saved=await saveRecord('lots',value);setValue(saved);setDirty(false);setDrawing(false);await onReload();}catch(e){setError((e as Error).message);}finally{setBusy(false);}}
  return <div><div className="editor-top"><div><p className="eyebrow">PLANO Y DISPONIBILIDAD</p><h1>{dev.title}</h1></div><button className="secondary" onClick={()=>{if(!dirty||confirm('¿Salir sin guardar?'))onDone();}}>Volver</button></div><div className="legend"><span className="available">Disponible</span><span className="sold">Vendido</span></div><div className="lot-workspace"><div><PlanView plan={dev.plan} lots={lots.filter(l=>l.id!==value?.id||!drawing)} selected={value?.id} onSelect={choose} onPoint={drawing?(p)=>change({polygon:[...(value?.polygon||[]),p]}):undefined} draft={drawing?value?.polygon:[]}/>{drawing&&<div className="notice">Marca las esquinas del lote, en orden. Mínimo tres puntos. <button type="button" className="text-button" onClick={()=>change({polygon:value?.polygon.slice(0,-1)})}>Deshacer punto</button><button type="button" className="text-button" onClick={()=>setDrawing(false)}>Terminar contorno</button></div>}<div className="lot-buttons">{lots.map(l=><button key={l.id} onClick={()=>choose(l)}>M{l.block} · L{l.number}<small>{l.status}</small></button>)}</div><button className="button" onClick={()=>choose(blankLot(dev.id))}>+ Agregar lote</button></div><aside className="lot-form">{value?<form onSubmit={submit}><h2>{value.revision?`Lote ${value.number}`:'Nuevo lote'}</h2><fieldset disabled={busy}><div className="form-grid"><Field label="Manzana"><input value={value.block} required maxLength={50} onChange={e=>change({block:e.target.value})}/></Field><Field label="Número de lote"><input value={value.number} required maxLength={50} onChange={e=>change({number:e.target.value})}/></Field><NumberField label="Superficie (m²)" value={value.area} onChange={v=>change({area:v})}/><NumberField label="Precio (MXN)" value={value.price} onChange={v=>change({price:v})}/></div><Select label="Disponibilidad" value={value.status} options={['Disponible','Vendido']} onChange={v=>change({status:v as Lot['status']})}/>{value.status==='Vendido'&&<><Field label="Venta realizada por"><select required value={value.soldBy} onChange={e=>change({soldBy:e.target.value as Lot['soldBy']})}><option value="">Seleccionar</option><option>Mexino</option><option>Tercero</option></select></Field><Field label="Quién reportó la venta"><input maxLength={200} value={value.reportedBy} onChange={e=>change({reportedBy:e.target.value})}/></Field><Field label="Fecha reportada"><input type="date" value={value.reportedDate} onChange={e=>change({reportedDate:e.target.value})}/></Field>{value.soldBy==='Mexino'&&<p className="notice">Aquí se actualiza disponibilidad. El expediente del comprador y las condiciones de pago se incorporan en la segunda etapa.</p>}</>}<Field label="Nota interna"><textarea rows={3} maxLength={3000} value={value.note} onChange={e=>change({note:e.target.value})}/></Field><button type="button" className="secondary" disabled={!dev.plan} onClick={()=>{if(value.polygon.length&&!confirm('¿Volver a dibujar el contorno de este lote?'))return;change({polygon:[]});setDrawing(true);}}>Dibujar contorno</button><p className="muted">{value.polygon.length} puntos · La comisión predeterminada es {dev.commissionType==='Porcentaje'?`${dev.commissionValue}%`:money(dev.commissionValue)}.</p>{error&&<p className="error" role="alert">{error}</p>}<button className="button" disabled={busy||drawing}>{busy?'Guardando…':'Guardar lote'}</button></fieldset></form>:<div className="empty">Selecciona un lote o agrega uno nuevo.</div>}</aside></div></div>;
}
export default function Admin(){
  const [user,setUser]=useState<User|null>(null),[loading,setLoading]=useState(true),[authError,setAuthError]=useState(''),[data,setData]=useState<Catalog>(emptyCatalog),[error,setError]=useState(''),[section,setSection]=useState('Resumen'),[query,setQuery]=useState(''),[editor,setEditor]=useState<Property|Development|null>(null),[dev,setDev]=useState<Development|null>(null),[audit,setAudit]=useState<Audit[]>([]);
  async function reload(){try{setData(await api<Catalog>('/api/admin/catalog'));setError('');}catch(e){setError((e as Error).message);throw e;}}
  useEffect(()=>{api<User>('/api/admin/session').then(u=>{setUser(u);return reload();}).catch(e=>setAuthError((e as Error).message)).finally(()=>setLoading(false));},[]);
  function go(s:string){setSection(s);setQuery('');setEditor(null);setDev(null);if(s==='Historial')api<Audit[]>('/api/admin/audit').then(setAudit).catch(e=>setError((e as Error).message));}
  const done=()=>{setEditor(null);setDev(null);void reload().catch(()=>{});};
  if(loading)return <div className="login-page"><Brand/><p role="status">Verificando acceso…</p></div>;
  if(!user)return <div className="login-page"><Brand/><div className="login-box"><p className="eyebrow">ESPACIO DEL EQUIPO</p><h1>Administración<br/>Casa Mexino</h1><p>Accede con tu correo autorizado para administrar propiedades y fraccionamientos.</p><p className="notice" role="status">{authError}</p><a className="button" href="/admin/">Iniciar sesión / Reintentar</a><a className="back" href="/">← Volver al portal</a></div></div>;
  return <div className="admin-shell"><aside className="sidebar"><Brand/><p className="eyebrow">COMERCIAL</p><nav>{['Resumen','Propiedades','Fraccionamientos',...(user.role==='Administrador'?['Historial']:[])].map(s=><button key={s} className={s===section?'active':''} disabled={!!editor||!!dev} onClick={()=>go(s)}>{s}</button>)}</nav><div className="sidebar-bottom"><span>{user.role}</span><small>{user.email}</small><a href="/" target="_blank" rel="noreferrer">Ver portal ↗</a><a href="/cdn-cgi/access/logout">Cerrar sesión</a></div></aside><div className="admin-main"><header className="admin-topbar"><span>Espacio de trabajo / {section}</span><span>Casa Mexino</span></header><div className="admin-content">{error&&<div className="error" role="alert">{error} <button onClick={()=>void reload().catch(()=>{})}>Reintentar</button></div>}
    {editor?(section==='Propiedades'?<PropertyEditor initial={editor as Property} onDone={done}/>:<DevelopmentEditor initial={editor as Development} onDone={done}/>):dev?<LotsEditor dev={dev} lots={data.lots.filter(l=>l.developmentId===dev.id)} onDone={done} onReload={reload}/>:<>
      <div className="section-top"><div><p className="eyebrow">ADMINISTRACIÓN</p><h1>{section==='Resumen'?'Tu inventario, al día.':section}</h1></div>{section==='Propiedades'&&<button className="button" onClick={()=>setEditor(blankProperty())}>+ Nueva propiedad</button>}{section==='Fraccionamientos'&&<button className="button" onClick={()=>setEditor(blankDevelopment())}>+ Nuevo fraccionamiento</button>}</div>
      {section==='Resumen'&&<><div className="metrics"><div><span>Propiedades publicadas</span><strong>{data.properties.filter(p=>p.publication==='Publicado').length}</strong></div><div><span>Fraccionamientos</span><strong>{data.developments.length}</strong></div><div><span>Lotes disponibles</span><strong>{data.lots.filter(l=>l.status==='Disponible').length}</strong></div><div><span>Lotes vendidos</span><strong>{data.lots.filter(l=>l.status==='Vendido').length}</strong></div></div><div className="start-actions"><button onClick={()=>{setSection('Propiedades');setEditor(blankProperty());}}><span>01</span><h2>Publicar una propiedad</h2><p>Fotos, precio, características y ubicación.</p></button><button onClick={()=>{setSection('Fraccionamientos');setEditor(blankDevelopment());}}><span>02</span><h2>Agregar un fraccionamiento</h2><p>Plano, lotes y disponibilidad.</p></button></div><div className="notice">Primera etapa: inventario y publicación. Ventas, cobranza, entregas a propietarios y caja se incorporarán en la siguiente etapa.</div></>}
      {['Propiedades','Fraccionamientos'].includes(section)&&<><Field label="Buscar por nombre"><input className="admin-search" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Buscar…"/></Field><div className="table-scroll"><table><thead><tr><th>{section==='Propiedades'?'Propiedad':'Fraccionamiento'}</th><th>{section==='Propiedades'?'Precio':'Lotes'}</th><th>Publicación</th><th>Acciones</th></tr></thead><tbody>{(section==='Propiedades'?data.properties:data.developments).filter(p=>p.title.toLowerCase().includes(query.toLowerCase())).map(p=><tr key={p.id}><td><strong>{p.title}</strong><small>{p.address}</small>{'status' in p&&<span className="pill">{p.status}</span>}</td><td>{'price' in p?money(p.price):data.lots.filter(l=>l.developmentId===p.id).length}</td><td><span className={'pill '+(p.publication==='Publicado'?'published':'')}>{p.publication}</span></td><td><button className="text-button" onClick={()=>setEditor(p)}>Editar</button>{section==='Fraccionamientos'&&<button className="text-button" onClick={()=>setDev(p as Development)}>Plano y lotes</button>}</td></tr>)}</tbody></table>{!(section==='Propiedades'?data.properties:data.developments).length&&<div className="empty">Todavía no hay registros. Agrega el primero para comenzar.</div>}</div></>}
      {section==='Historial'&&<div className="table-scroll"><table><thead><tr><th>Fecha</th><th>Usuario</th><th>Acción</th><th>Registro</th></tr></thead><tbody>{audit.map(a=><tr key={a.id}><td>{a.created_at} UTC</td><td>{a.actor}</td><td>{a.action}</td><td>{[...data.properties,...data.developments,...data.lots].find(r=>r.id===a.record_id)?.id.slice(0,8)||a.record_id.slice(0,8)}</td></tr>)}</tbody></table>{!audit.length&&<div className="empty">No hay cambios registrados.</div>}</div>}
    </>}</div></div></div>;
}
